package {{.Package}}

import "time"

// {{.Entity | ToPascalCase}} is the {{.Entity | ToLower}} domain model.
type {{.Entity | ToPascalCase}} struct {
	ID        int64     `json:"id"`
	Name      string    `json:"name"`
	CreatedAt time.Time `json:"created_at"`
	UpdatedAt time.Time `json:"updated_at"`
}
