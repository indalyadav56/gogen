import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// Builds to web/dist, which the Go backend embeds and serves. During `npm run
// dev`, Vite proxies /api to the Go server so you can develop both together.
export default defineConfig({
  plugins: [react()],
  build: {
    outDir: 'dist',
    emptyOutDir: true,
  },
  server: {
    proxy: {
      '/api': 'http://localhost:8080',
    },
  },
})
